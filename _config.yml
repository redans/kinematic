remote_theme: pages-themes/midnight@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

title: Self-centering of a Kinematic body
description: calculating self-centering of a 6DOF kinematic suspended body

