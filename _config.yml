remote_theme: pages-themes/midnight@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

title: Self-centering of a Kinematic body
description: calculating self-centering of a kinematic body suspended on a 6DOF mount

